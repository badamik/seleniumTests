package com.idium.editap.selenium;

import io.ddavison.conductor.Browser;
import io.ddavison.conductor.Config;


@Config(
        browser = Browser.CHROME,
        url = "http://dev-strype.idium.no/travelsome"
)

/**
 * Created by Bartek on 23.05.2017.
 */
public class SolutionSettingsTest extends Login {
/*
    @Test
    public void openSolutionSettingsTest() {
        //driver.manage().timeouts().pageLoadTimeout(10, TimeUnit.SECONDS);
        //validatePresent(HomePage.LOC_BTN_THUMBMENU);

        //click(HomePage.LOC_BTN_THUMBMENU);

        validatePresent(AdminInterfaceElements.LOC_BTN_THUMBMENU_SOLUTIONSETTINGS);
        click(AdminInterfaceElements.LOC_BTN_THUMBMENU_SOLUTIONSETTINGS);
        validatePresent((AdminInterfaceElements.LOC_DIV_SOLUTIONSETTINGS));

        //validatePresent(HomePage.LOC_BTN_PAGESETTINGS);
        //click(HomePage.LOC_BTN_PAGESETTINGS);
    }

    @Test
    public void openpageSettingsTest() {
        //driver.manage().timeouts().pageLoadTimeout(10, TimeUnit.SECONDS);
        //validatePresent(HomePage.LOC_BTN_THUMBMENU);

        //click(HomePage.LOC_BTN_THUMBMENU);

        validatePresent(AdminInterfaceElements.LOC_BTN_THUMBMENU_SOLUTIONSETTINGS);
        click(AdminInterfaceElements.LOC_BTN_THUMBMENU_SOLUTIONSETTINGS);
        validatePresent((AdminInterfaceElements.LOC_DIV_SOLUTIONSETTINGS));

        //validatePresent(AdminInterfaceElements.LOC_BTN_PAGESETTINGS);
        //click(AdminInterfaceElements.LOC_BTN_PAGESETTINGS);
    }
*/
}
